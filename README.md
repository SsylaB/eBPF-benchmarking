# eBPF-benchmarking


## Dépendances

Ce repo utilise libbpf et bpftool comme submodules git.
Cloner avec :
```bash
git clone --recurse-submodules https://github.com/SsylaB/eBPF-benchmarking
```

Ou si déjà cloné sans submodules :
```bash
git submodule update --init --recursive
```
